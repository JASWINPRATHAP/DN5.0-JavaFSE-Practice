'use strict';
var $ = require('core-js-pure/internals/export');
var lengthOfArrayLike = require('core-js-pure/internals/length-of-array-like');
var toIndexedObject = require('core-js-pure/internals/to-indexed-object');
var createProperty = require('core-js-pure/internals/create-property');
var addToUnscopables = require('core-js-pure/internals/add-to-unscopables');

var $Array = Array;

// `Array.prototype.toReversed` method
// https://tc39.es/ecma262/#sec-array.prototype.toreversed
$({ target: 'Array', proto: true }, {
  toReversed: function toReversed() {
    var O = toIndexedObject(this);
    var len = lengthOfArrayLike(O);
    var A = new $Array(len);
    var k = 0;
    for (; k < len; k++) createProperty(A, k, O[len - k - 1]);
    return A;
  }
});

addToUnscopables('toReversed');
