'use strict';
var $ = require('core-js-pure/internals/export');
var call = require('core-js-pure/internals/function-call');
var isObject = require('core-js-pure/internals/is-object');
var anObject = require('core-js-pure/internals/an-object');
var isDataDescriptor = require('core-js-pure/internals/is-data-descriptor');
var getOwnPropertyDescriptorModule = require('core-js-pure/internals/object-get-own-property-descriptor');
var getPrototypeOf = require('core-js-pure/internals/object-get-prototype-of');
var toPropertyKey = require('core-js-pure/internals/to-property-key');

// `Reflect.get` method
// https://tc39.es/ecma262/#sec-reflect.get
var $get = function (target, propertyKey, receiver) {
  if (anObject(target) === receiver) return target[propertyKey];
  var descriptor = getOwnPropertyDescriptorModule.f(target, propertyKey);
  if (descriptor) return isDataDescriptor(descriptor)
    ? descriptor.value
    : descriptor.get === undefined ? undefined : call(descriptor.get, receiver);
  var prototype = getPrototypeOf(target);
  if (isObject(prototype)) return $get(prototype, propertyKey, receiver);
};

$({ target: 'Reflect', stat: true }, {
  get: function get(target, propertyKey /* , receiver */) {
    return $get(anObject(target), toPropertyKey(propertyKey), arguments.length < 3 ? target : arguments[2]);
  }
});
